using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "AccessoryData", menuName = "CharacterSkin/AccessoryData", order = 1)]
public class AccessoryData : CharacterSkin<Accessory>
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
